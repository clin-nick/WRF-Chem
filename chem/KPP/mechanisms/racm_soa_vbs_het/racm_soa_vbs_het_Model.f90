MODULE racm_soa_vbs_het_Model

!~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
!  Completely defines the model racm_soa_vbs_het
!    by using all the associated modules
!~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

  USE racm_soa_vbs_het_Precision
  USE racm_soa_vbs_het_Parameters
  USE racm_soa_vbs_het_Global
  USE racm_soa_vbs_het_Function
  USE racm_soa_vbs_het_Integrator
  USE racm_soa_vbs_het_Rates
  USE racm_soa_vbs_het_Jacobian
  USE racm_soa_vbs_het_LinearAlgebra
  USE racm_soa_vbs_het_Monitor
  USE racm_soa_vbs_het_Util

END MODULE racm_soa_vbs_het_Model

